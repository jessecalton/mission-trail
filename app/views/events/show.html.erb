<script src="/assets/_game_event.js"></script>

<div class="header">
  <h1>Local Wisdom</h1>
</div>

<div class="gamepage-link">
  <% @event.options.each_with_index do |option, val| %>

    <%= link_to option.text, event_result_path(@event, @event.results[val].id) %>
  <% end %>
</div>

